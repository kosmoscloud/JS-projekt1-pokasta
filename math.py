class calculations():

    def elementy(self, wartosci, ilosci):
        licznik = 0
        mianownik = 0
        for i in (0, 12):
            licznik += wartosci[i]
            mianownik += ilosci[i]
        return licznik, mianownik

    def odchylLicz(self, ):

    def elementy2(self, wartosci, ilosci, srednia):
        wspasLicz = 0
        eksLicz = 0
        for i in (0, 12):
            print("b?")